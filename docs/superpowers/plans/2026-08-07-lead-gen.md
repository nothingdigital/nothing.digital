# Lead Gen Hybrid Activation — Implementation Plan

> **For agentic workers:** Implement task-by-task. Steps use checkbox syntax.

**Goal:** Ship inbound conversion fixes + docs so owner can activate Instantly/Listmonk in parallel.

**Architecture:** Small PRs on existing contact/email/homepage paths. No new queue or services.

**Tech Stack:** Next.js App Router, Resend templates, Umami `data-umami-event` / `umami.track`, Vitest.

---

### Task 1: Email templates — Calendly, honest nurture, footers

**Files:**

- Modify: `src/lib/email/templates.ts`
- Modify: `src/app/api/contact/route.ts` (pass calendly into confirmation)
- Test: `src/app/api/contact/route.test.ts`

- [x] Confirmation accepts `calendlyUrl` and includes Book CTA when set
- [x] Nurture Day-0 drops false Day 3/7 + showcase spam
- [x] Footer drops n8n marketing copy
- [x] Team notify always sent (remove `!isN8nConfigured` gate)
- [x] Tests pass

### Task 2: Contact success UI + Calendly prop + Umami

**Files:**

- Modify: `src/app/(site)/contact/components/contact-form.tsx`
- Modify: `src/app/(site)/contact/page.tsx`
- Modify: newsletter form + calendly links for events

- [x] Pass `calendlyUrl` into ContactForm; success CTA books call
- [x] Track `contact_submit` on success; `calendly_click` on Calendly links
- [x] Track `newsletter_subscribe` on newsletter success

### Task 3: Remove AvailabilityWidget

**Files:**

- Modify: `src/app/(site)/page.tsx`
- Delete: `src/components/molecules/availability-widget.tsx`

- [x] Done

### Task 4: Docs + SCRATCHPAD

**Files:**

- Modify: `docs/conversion-boost.md` (nurture = Day-0 only; widget deleted)
- Modify: `SCRATCHPAD.md`

- [x] Done

### Task 5: Verify

- [x] `pnpm test` for contact route + contact form + newsletter form
